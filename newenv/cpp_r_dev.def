Bootstrap: docker
From: ubuntu:24.04
Stage: spython-base

%post
# Use Ubuntu 24.04 as base image

# Avoid interactive prompts during installation
DEBIAN_FRONTEND=noninteractive

# Update package list and install dependencies
apt update && apt install -y \
build-essential \
libeigen3-dev \
cmake \
pkg-config \
&& rm -rf /var/lib/apt/lists/*

# # Updating gcc/g++
apt-get update
apt install software-properties-common -y
apt install g++-14 -y

# # Installing starship: https://starship.rs/guide/
apt install curl
cd /tmp && curl -sS https://starship.rs/install.sh > install_starship.sh  &&  sh install_starship.sh --yes
echo 'eval "$(starship init bash)"' >> ~/.bashrc

apt install r-base -y

R
install.packages('bipartite', repos = 'https://www.icesi.edu.co/CRAN/')
install.packages('Rcpp', repos = 'https://www.icesi.edu.co/CRAN/')
install.packages('RInside', repos = 'https://www.icesi.edu.co/CRAN/')
quit()
y

# Set working directory
mkdir -p /workspace
cd /workspace

# Default command
%environment
export DEBIAN_FRONTEND=noninteractive
%runscript
cd /workspace
exec /bin/bash /bin/bash "$@"
%startscript
cd /workspace
exec /bin/bash /bin/bash "$@"
